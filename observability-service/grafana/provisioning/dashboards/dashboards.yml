apiVersion: 1

providers:
  - name: PixelPlatform
    orgId: 1
    folder: PixelPlatform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
